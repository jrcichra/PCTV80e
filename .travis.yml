matrix:
  include:
    - os: linux
      dist: precise
    - os: linux
      dist: trusty
    - os: linux
      dist: xenial
    - os: linux
      dist: bionic
language: c
install:
  - git clone git://linuxtv.org/media_build.git
  - cd media_build
  - chmod +x build.sh
  - ./build